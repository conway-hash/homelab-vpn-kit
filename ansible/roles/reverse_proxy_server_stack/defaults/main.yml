---
# REQUIRED, no safe default: DNS-01 needs API access to the real
# conway-hash.com zone to prove ownership of names under
# {{ tailnet_base_domain }}. Scope this token to Zone:DNS:Edit on the
# conway-hash.com zone only — see SETUP.md.
#
# stack_dir/caddy_version/tailnet_hostname are declared exactly once, in
# group_vars/reverse_proxy_server/vars.yml — not restated here.
# acme_email lives in group_vars/all/vars.yml — shared with the
# coordination server's Caddy, not repeated per machine.
cloudflare_api_token: ""
