---
# domain isn't declared here — it's derived from headscale_server_url (see
# group_vars/all/vars.yml) in coordination_server_stack's defaults, since
# they're the same fact in two forms (bare hostname vs. full URL) and
# declaring both independently let them drift once already.
stack_dir: /opt/coordination-stack
headscale_version: "0.29.3"
headplane_version: "0.7.0"
caddy_version: "2.11.4-alpine"
