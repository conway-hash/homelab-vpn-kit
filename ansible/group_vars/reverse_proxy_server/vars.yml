---
stack_dir: /opt/reverse_proxy_server_stack
caddy_version: "2.11.4"

# Deliberately short and NOT matching this repo's own (long) name — this
# becomes the real public hostname, proxy.ts.conway-hash.com. "proxy" over
# "edge" on purpose: says what it does, not infra jargon — applied
# everywhere now (proxy_vm_name, ENABLE_PROXY, etc.), not just here. Must
# match proxy_vm_name's default in terraform/variables.tf, or the VM
# Proxmox creates and the name it presents to the tailnet disagree.
tailnet_hostname: proxy
